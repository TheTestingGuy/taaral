---
// src/pages/projects.astro
import Layout from "@layouts/Layout.astro";
import { getCollection } from "astro:content";
import { Image } from "astro:assets";
import ProjectsHero from "@components/ProjectsHero.astro";

// 1. On récupère tous les projets et on les trie par 'order'
const allProjects = await getCollection("projects");
const sortedProjects = allProjects.sort((a, b) => a.data.order - b.data.order);

// 2. On définit nos 5 styles de cartes asymétriques pour recréer le design de Stitch
const cardStyles = [
  { wrapper: "md:col-span-7", aspect: "aspect-[4/3]" },
  { wrapper: "md:col-span-5 md:mt-24", aspect: "aspect-[3/4]" },
  { wrapper: "md:col-span-5", aspect: "aspect-square" },
  { wrapper: "md:col-span-7 md:mt-16", aspect: "aspect-[16/10]" },
  { wrapper: "col-span-1 md:col-span-12 mt-12", aspect: "aspect-[21/9]" }, // Pleine largeur
];
---

<Layout title="Material Creative - Projects">
  <div class="mx-auto w-full max-w-[1920px] px-4 pb-20 md:px-8 lg:px-16">
    <ProjectsHero />

    <section
      class="grid grid-cols-1 items-start gap-x-8 gap-y-16 md:grid-cols-12 md:gap-x-12"
    >
      {
        sortedProjects.map((project, index) => {
          // Modulo 5 permet de répéter le pattern si on a plus de 5 projets !
          const style = cardStyles[index % 5];
          const delayClass = `delay-${((index % 4) + 1) * 100}`; // Génère delay-100, delay-200...

          return (
            <a
              href={`/projects/${project.slug}`}
              class={`block ${style.wrapper} fade-in-up group cursor-pointer ${delayClass}`}
            >
              {" "}
              <div
                class={`relative overflow-hidden rounded-sm ${style.aspect} mb-4`}
              >
                <Image
                  src={project.data.image}
                  alt={project.data.title}
                  class="image-hover-zoom size-full object-cover"
                  transition:name={`image-${project.slug}`}
                />
              </div>
              <div class="mt-2 flex flex-col justify-between border-t border-primary-projects/20 pt-4 md:flex-row md:items-baseline">
                <h3 class="font-display text-2xl italic text-primary-projects transition-colors group-hover:text-accent md:text-3xl">
                  {project.data.title}
                </h3>
                <p class="mt-1 text-sm uppercase tracking-widest text-primary-projects/60 md:mt-0">
                  {project.data.location}
                </p>
              </div>
            </a>
          );
        })
      }
    </section>
  </div>
</Layout>

<style>
  .fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .delay-100 {
    animation-delay: 0.1s;
  }
  .delay-200 {
    animation-delay: 0.2s;
  }
  .delay-300 {
    animation-delay: 0.3s;
  }
  .delay-400 {
    animation-delay: 0.4s;
  }

  .image-hover-zoom {
    transition: transform 0.7s ease;
  }
  .group:hover .image-hover-zoom {
    transform: scale(1.03);
  }
</style>
